# Open questions — systems evidence

Things that are **true in repos or `proof.ts` but must not appear as live screenshots or client claims** until confirmed.

## Missing gallery PNG files

`src/content/proof.ts` `leadMagnetGallery` points at:

- `/gratka/lead-magnet-start.png`
- `/gratka/lead-magnet-entry-gate.png`
- (further `lead-magnet-*.png` rows)

**None of these files exist in `public/gratka/`** (directory is SVG/markdown only).  
Lead Scout therefore ships a **drawn SVG schematic**, not product screenshots. Do not publish missing PNGs.

## Telegram approval

`jadzia-coi.ts` mentions Telegram HITL. That is **not** on `/systems/owner-cockpit/` until someone confirms the same gate exists in `dsaas-platform-main` as a client-facing pattern we are willing to sell. Until then: approval queue on the cockpit page only.

## Social automation

Not one of the six spokes. Publishing Gate FAQ states live social cadence stays off until a written unlock. Do not fold social-automation into these pages.

## FlexGrafik / Wizard / game

First lab tenant that left. Status elsewhere: `LAB: PAUSED`.  
Do not call zzpackage, the game, or FlexGrafik a QuietForge commercial success or “live proof”.

## Telegram / HITL word

Public copy uses **human approval** / **approval gate**. Never HITL, Jadzia, COI, VCMS, KODA, DSAAS on spokes.

## Client-verified numbers

None. Cost facts are `Lab measurement` or `Illustrative example` only. Do not invent `Client-verified`.
